# Script-Creator 多样化智能匹配 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 将多样化智能匹配设计规格整合到 script-creator SKILL.md 中，实现分层推荐结构和亲和度映射机制。

**架构：** 在现有阶段2（十维度多样化选择）基础上，增加综合评分引擎、分层推荐结构、亲和度映射表、场景智能推导规则，以及增强的输出格式。

**设计规格参考**: `docs/superpowers/specs/2026-03-29-script-creator-diversity-matching-design.md`

**技术栈：** Markdown 技能文档

---

## 文件结构

| 文件 | 职责 |
|------|------|
| `script-creator/SKILL.md` | 主技能文档，需修改阶段2内容，增加智能匹配机制 |

---

## 任务 1：更新执行流程图和快速导航

**文件：**
- 修改：`script-creator/SKILL.md:53-105`（执行流程图）

- [ ] **步骤 1：更新快速导航**

将阶段2的描述更新为：
```
│  ├── 【阶段2：十维度多样化选择】→ 第120行（含智能匹配机制）│
```

- [ ] **步骤 2：更新执行流程图**

在阶段2的流程框中增加智能匹配步骤：

```markdown
┌─────────────────────────────────────────────────────────────┐
│ 阶段2：十维度多样化选择（智能匹配版）                        │
│  ├── 预处理：综合评分引擎                                    │
│  │   ├── 判断五因素特征                                      │
│  │   └── 输出创意适配度等级                                  │
│  ├── 维度1：脚本类型（独立维度）                             │
│  ├── 第1层：核心维度（一致性层）                             │
│  │   ├── 维度4：情绪基调                                    │
│  │   ├── 维度5：视觉风格                                    │
│  │   └── 场景设定（智能推导）                                │
│  ├── 第2层：表达维度（表达层）                               │
│  │   ├── 维度2：开场钩子类型                                │
│  │   ├── 维度3：叙事结构                                    │
│  │   ├── 维度8：节奏模板                                    │
│  │   └── 维度7：台词风格                                    │
│  └── 第3层：创意维度（突破层）                               │
│      ├── 维度10：创新元素                                   │
│      ├── 维度9：完播钩子设计                                │
│      └── 维度6：镜头语言                                    │
└─────────────────────────────────────────────────────────────┘
```

- [ ] **步骤 3：Commit**

```bash
git add script-creator/SKILL.md
git commit -m "feat(script-creator): 更新执行流程图，增加智能匹配结构"
```

---

## 任务 2：增加综合评分引擎章节（含判定示例）

**文件：**
- 修改：`script-creator/SKILL.md:148`（阶段2开头位置）

- [ ] **步骤 1：在阶段2标题后插入综合评分引擎章节**

在 `## 阶段2：十维度多样化选择` 行后，`**核心原则**` 行前插入：

```markdown
---

### 2.0 综合评分引擎（智能匹配预处理）

在开始十维度选择前，先判断创意适配度等级，决定离域倾向。

#### 五因素定性判断标准

| 因素 | 权重优先级 | 低特征 | 中特征 | 高特征 |
|------|------------|--------|--------|--------|
| **情绪复杂度** | 1（最高） | 单一情绪 | 复合情绪 | 矛盾情绪 |
| **热点新鲜度** | 2 | 常见话题 | 中等新颖 | 新颖话题 |
| **服饰风格张力** | 3 | 基础款/百搭 | 有风格但不强烈 | 强风格/个性款 |
| **场景约束强度** | 4 | 场景适配范围窄 | 中等范围 | 场景适配范围宽 |
| **受众年龄层** | 5（最低） | 成熟受众（30+） | 混合受众 | 年轻受众（18-25） |

#### 创意适配度等级判定表

**判定逻辑**：按权重优先级依次判断，高权重因素主导结果。

| 判定条件 | 创意适配度等级 | 离域倾向建议 |
|----------|----------------|--------------|
| 情绪复杂度=高 | **大胆** | 突破层鼓励多离域组合，表达层可离域2项 |
| 情绪复杂度=中 且 热点新鲜度=高 | **大胆** | 突破层鼓励多离域组合，表达层可离域2项 |
| 情绪复杂度=中 且 热点新鲜度=中 | **中等** | 突破层可离域2项，表达层可离域1项 |
| 情绪复杂度=中 且 热点新鲜度=低 | **保守** | 突破层可离域1项，表达层不离域 |
| 情绪复杂度=低 且 (热点新鲜度=高 或 服饰风格张力=高) | **中等** | 突破层可离域2项，表达层可离域1项 |
| 情绪复杂度=低 且 热点新鲜度=中 | **保守** | 突破层可离域1项，表达层不离域 |
| 情绪复杂度=低 且 热点新鲜度=低 | **保守** | 突破层可离域1项，表达层不离域 |

#### 判定示例

**示例1**：
- 情绪：治愈中带自足 → 情绪复杂度=中
- 热点：独居生活治愈瞬间 → 热点新鲜度=低（常见话题）
- 服饰：米色慵懒风卫衣 → 服饰风格张力=中

**判定**：情绪复杂度=中 且 热点新鲜度=低 → **保守**
- 突破层：可离域1项
- 表达层：不离域
- 核心层：不离域

**示例2**：
- 情绪：想逃离又不敢失去 → 情绪复杂度=高（矛盾情绪）
- 热点：职场安全感危机 → 热点新鲜度=中
- 服饰：通勤职场风西装 → 服饰风格张力=中

**判定**：情绪复杂度=高 → **大胆**
- 突破层：鼓励多离域组合
- 表达层：可离域2项
- 核心层：不离域

#### 三层离域倾向等级

| 层级 | 离域倾向 | 判定规则 |
|------|----------|----------|
| 核心层 | 极低 | 无论创意适配度如何，核心层都极少偏离，保一致性 |
| 表达层 | 中等 | 保守→不离域，中等→可离域1项，大胆→可离域2项 |
| 突破层 | 高 | 保守→可离域1项，中等→可离域2项，大胆→鼓励多离域组合 |

#### 连续生成离域触发规则

当用户请求连续生成多条脚本时，应用确定性离域触发规则：

| 连续生成序号 | 离域触发规则 |
|--------------|--------------|
| 第1条 | 按创意适配度等级判定 |
| 第2条 | 表达层强制离域1项（轮换） |
| 第3条 | 突破层强制离域组合 |
| 第4条+ | 全层轮换：核心层可微调，表达层换项，突破层换组合 |

```

- [ ] **步骤 2：Commit**

```bash
git add script-creator/SKILL.md
git commit -m "feat(script-creator): 增加综合评分引擎章节（含判定示例）"
```

---

## 任务 3：增加场景智能推导规则

**文件：**
- 修改：`script-creator/SKILL.md:131-145`（阶段1·热点-角色匹配校验位置）

- [ ] **步骤 1：替换原有的 1.3 热点-角色匹配校验**

将原有的 `### 1.3 热点-角色匹配校验` 替换为：

```markdown
### 1.3 场景智能推导

**上游来源**：
- 必需：热点分析·推演场景
- 必需：角色分析·服饰风格分类

**匹配逻辑**：

```
步骤1：提取热点推演场景关键词
步骤2：提取服饰风格分类
步骤3：查"服饰风格→适配场景"映射表 → 获取适配场景列表
步骤4：计算热点推演场景与适配场景的兼容性
步骤5：输出推荐场景（兼容性最高的场景）
步骤6：若有冲突，执行场景调整
```

**服饰风格→适配场景 映射表**：

| 服饰风格分类 | 默认适配场景（高匹配） | 扩展适配场景（中匹配） | 不适配场景 |
|--------------|------------------------|------------------------|------------|
| **慵懒家居风** | 居家、阳台、沙发、窗边 | 咖啡店、书店、周末休闲 | 办公室、商务场合 |
| **通勤职场风** | 办公室、地铁站、写字楼 | 商务餐厅、城市街景 | 居家、运动场 |
| **文艺森系风** | 书店、咖啡馆、公园、画展 | 居家窗边、校园 | 健身房、夜店 |
| **休闲街头风** | 街头、商场、朋友聚会 | 咖啡店、公园、校园 | 正式场合、婚礼 |
| **优雅知性风** | 画廊、餐厅、高端场所 | 咖啡店、书店、剧场 | 运动场、菜市场 |
| **浪漫约会风** | 餐厅、电影院、花店 | 咖啡店、公园、街景 | 办公室、健身房 |

**场景冲突处理规则**：

| 冲突类型 | 处理策略 | 示例 |
|----------|----------|------|
| **完全冲突** | 热点场景与服饰不适配场景完全重叠 | 提示用户，建议调整热点或服饰，或调整场景保留情绪内核 |
| **部分冲突** | 热点场景与服饰有部分兼容 | 调整场景描述，保留情绪内核 |
| **无冲突** | 热点场景与服饰高匹配场景兼容 | 直接匹配使用 |

**推导示例**：
```
热点推演场景：阳光窗边
服饰风格：慵懒家居风

兼容性匹配：
├── "窗边" ← 热点关键词 ✓ 服饰高匹配场景 ✓ → 直接匹配
├── "居家" ← 热点暗示 ✓ 服饰高匹配场景 ✓ → 高兼容
└── "咖啡店" ← 热点关键词不匹配 ✗ → 低兼容

推荐场景设定：居家窗边角落(★★★★★)
```
```

- [ ] **步骤 2：Commit**

```bash
git add script-creator/SKILL.md
git commit -m "feat(script-creator): 增加场景智能推导规则"
```

---

## 任务 4：增加维度1脚本类型亲和度映射

**文件：**
- 修改：`script-creator/SKILL.md:152-168`（维度一脚本类型位置）

- [ ] **步骤 1：在维度一脚本类型章节末尾增加映射规则**

在 `**重要**：脚本类型仅影响叙事风格和镜头语言...` 行后插入：

```markdown

---

#### 脚本类型亲和度映射规则

**上游来源**：热点分析·推荐脚本类型 + 角色分析·服饰风格分类

**特点**：优先遵循热点推荐，但可被服饰约束调整

> **注**：以下为核心映射规则，完整映射表见设计规格文档。

| 热点推荐脚本类型 + 服饰风格 | 推荐脚本类型（优先级排序） | 约束调整说明 |
|------------------------------|---------------------------|--------------|
| **氛围感/OOTD + 慵懒家居风** | 氛围感/OOTD(★★★★★) | 完全匹配，无调整 |
| **氛围感/OOTD + 通勤职场风** | 氛围感/OOTD(★★★★★) | 完全匹配，无调整 |
| **日常Vlog + 慵懒家居风** | 日常Vlog(★★★★★) > 氛围感/OOTD(★★★★) | 完全匹配，氛围感作为备选 |
| **剧情/短剧 + 慵懒家居风** | 剧情/短剧(★★★) → 调整为日常Vlog(★★★★★) | 服饰约束强，调整脚本类型 |
| **剧情/短剧 + 通勤职场风** | 剧情/短剧(★★★★★) | 完全匹配，职场剧情适配 |
| **情侣/闺蜜 + 休闲街头风** | 情侣/闺蜜(★★★★★) > 日常Vlog(★★★★) | 完全匹配，街头聚会场景 |
| **季节/节日 + 文艺森系风** | 季节/节日(★★★★★) > 氛围感/OOTD(★★★★) | 完全匹配，文艺季节氛围 |
| **旅行/探店 + 休闲街头风** | 旅行/探店(★★★★★) > 日常Vlog(★★★★) | 完全匹配 |
| **旅行/探店 + 慵懒家居风** | 旅行/探店(★★★) → 调整为日常Vlog(★★★★) | 服饰约束，不建议旅行类 |

**脚本类型调整规则**：
```
规则1：热点推荐与服饰风格完全匹配 → 直接采用热点推荐
规则2：热点推荐与服饰风格部分匹配 → 热点推荐优先，但标注备选
规则3：热点推荐与服饰风格冲突 → 调整为服饰适配的脚本类型
```
```

- [ ] **步骤 2：Commit**

```bash
git add script-creator/SKILL.md
git commit -m "feat(script-creator): 增加维度1脚本类型亲和度映射规则"
```

---

## 任务 5：增加核心层维度亲和度映射表（维度4-5）

**分层归属**：核心层（一致性层）

**文件：**
- 修改：`script-creator/SKILL.md:199-224`（维度四情绪基调、维度五视觉风格位置）

- [ ] **步骤 1：在维度四情绪基调章节增加映射规则**

在 `**情绪组合技巧**...` 行后插入：

```markdown

---

#### 情绪基调亲和度映射规则

**上游来源**：热点分析·情绪分析 + 角色分析·情绪契合

> **注**：以下为核心映射规则，完整映射表见设计规格文档。

| 上游情绪特征 | 推荐情绪基调（优先级排序） | 离域选项（极少使用） |
|--------------|---------------------------|----------------------|
| **单一治愈** | 治愈温暖型(★★★★★) > 静谧内敛型(★★★★) | 反转型(离域★★) |
| **单一焦虑** | 热血励志型(★★★★★) > 感伤释怀型(★★★★) | 幽默轻松型(离域★★) |
| **单一静谧** | 静谧内敛型(★★★★★) > 治愈温暖型(★★★★) | 反转型(离域★★) |
| **治愈+自足** | 治愈温暖型(★★★★★) > 静谧内敛型(★★★★) | 反转型(离域★★) |
| **焦虑+坚韧** | 热血励志型(★★★★★) > 感伤释怀型(★★★★) | 幽默轻松型(离域★★) |
| **矛盾情绪** | 反转型(★★★★★) > 感伤释怀型(★★★★) | 治愈温暖型(离域★★) |

**亲和度星级含义**：
- ★★★★★：极高亲和（直接映射）
- ★★★★：高亲和（优先推荐）
- ★★★：中等亲和（可选）
- ★★：低亲和（离域探索）
```

- [ ] **步骤 2：在维度五视觉风格章节增加映射规则**

在视觉风格表格后插入：

```markdown

---

#### 视觉风格亲和度映射规则

**上游来源**：热点分析·推演风格 + 角色分析·服饰风格分类

> **注**：以下为核心映射规则，完整映射表见设计规格文档。

| 推演风格 + 服饰风格 | 推荐视觉风格（优先级排序） | 离域选项（极少使用） |
|---------------------|---------------------------|----------------------|
| **温暖治愈 + 慵懒家居风** | 居家温馨(★★★★★) > 日系清新(★★★★) | 都市夜色(离域★★) |
| **冷暖对比 + 通勤职场风** | 韩系精致(★★★★★) > 都市夜色(★★★★) | 居家温馨(离域★★) |
| **清新开阔 + 文艺森系风** | 日系清新(★★★★★) > 国风雅致(★★★★) | 欧美随性(离域★★) |
| **写实质朴 + 休闲街头风** | 欧美随性(★★★★★) > 日系清新(★★★) | 国风雅致(离域★★) |
| **温暖互动 + 优雅知性风** | 国风雅致(★★★★★) > 韩系精致(★★★★) | 欧美随性(离域★★) |
| **都市冷感 + 通勤职场风** | 都市夜色(★★★★★) > 韩系精致(★★★★) | 居家温馨(离域★★) |
```

- [ ] **步骤 3：Commit**

```bash
git add script-creator/SKILL.md
git commit -m "feat(script-creator): 增加核心层维度亲和度映射表（维度4-5）"
```

---

## 任务 6：增加表达层维度亲和度映射表（维度2-3-7-8）

**分层归属**：表达层

**文件：**
- 修改：`script-creator/SKILL.md:172-196`（维度二开场钩子、维度三叙事结构）
- 修改：`script-creator/SKILL.md:240-250`（维度七台词风格）
- 修改：`script-creator/SKILL.md:253-302`（维度八节奏模板）

- [ ] **步骤 1：在维度二开场钩子章节增加映射规则**

在 `**使用规则**...` 行后插入：

```markdown

---

#### 开场钩子亲和度映射规则

**上游来源**：第1层·情绪基调 + 热点分析·推荐钩子类型

> **注**：以下为核心映射规则，完整映射表见设计规格文档。

| 情绪基调 | 推荐开场钩子（优先级排序） | 离域选项（中等概率） |
|----------|---------------------------|----------------------|
| **治愈温暖型** | 视觉冲击型(★★★★★) > 情绪冲击型(★★★★) | 反差型(离域★★) |
| **热血励志型** | 情绪冲击型(★★★★★) > 冲突型(★★★★) | 视觉冲击型(离域★★) |
| **感伤释怀型** | 情绪冲击型(★★★★★) > 悬念型(★★★★) | 幽默型(离域★-不推荐) |
| **幽默轻松型** | 反差型(★★★★★) > 意外型(★★★★) | 情绪冲击型(离域★★) |
| **静谧内敛型** | 视觉冲击型(★★★★★) > 悬念型(★★★★) | 冲突型(离域★★) |
| **反转型** | 悬念型(★★★★★) > 意外型(★★★★★) | 视觉冲击型(离域★★) |
```

- [ ] **步骤 2：在维度三叙事结构章节增加映射规则**

在叙事结构表格后插入：

```markdown

---

#### 叙事结构亲和度映射规则

**上游来源**：维度1·脚本类型 + 第1层·情绪基调

> **注**：以下为核心映射规则，完整映射表（含情侣/闺蜜、季节/节日、旅行/探店组合）见设计规格文档。

| 脚本类型 + 情绪基调 | 推荐叙事结构（优先级排序） | 离域选项（中等概率） |
|---------------------|---------------------------|----------------------|
| **氛围感/OOTD + 治愈温暖** | 顺叙式(★★★★★) > 蒙太奇式(★★★★) | 倒叙式(离域★★) |
| **氛围感/OOTD + 静谧内敛** | 蒙太奇式(★★★★★) > 顺叙式(★★★★) | 倒叙式(离域★★) |
| **日常Vlog + 治愈温暖** | 顺叙式(★★★★★) > 循环式(★★★) | 倒叙式(离域★★) |
| **剧情/短剧 + 反转型** | 倒叙式(★★★★★) > 循环式(★★★★) | 蒙太奇式(离域★★) |
| **剧情/短剧 + 热血励志** | 顺叙式(★★★★★) > 倒叙式(★★★★) | 循环式(离域★★) |
| **剧情/短剧 + 感伤释怀** | 插叙式(★★★★★) > 倒叙式(★★★★) | 循环式(离域★★) |
```

- [ ] **步骤 3：在维度七台词风格章节增加映射规则**

在台词风格表格后插入：

```markdown

---

#### 台词风格亲和度映射规则

**上游来源**：第1层·情绪基调 + 角色分析·服饰调性

> **注**：以下为核心映射规则，完整映射表见设计规格文档。

| 情绪基调 + 服饰调性 | 推荐台词风格（优先级排序） | 离域选项（中等概率） |
|---------------------|---------------------------|----------------------|
| **治愈温暖 + 慵懒/温柔** | 内心独白型(★★★★★) > 无台词型(★★★★) | 对话型(离域★★) |
| **治愈温暖 + 文艺** | 内心独白型(★★★★★) > 旁白型(★★★★) | 网感型(离域★★) |
| **热血励志 + 干练** | 内心独白型(★★★★★) > 网感型(★★★) | 无台词型(离域★★) |
| **静谧内敛 + 慵懒** | 无台词型(★★★★★) > 内心独白型(★★★★) | 网感型(离域★-不推荐) |
| **感伤释怀 + 文艺/温柔** | 内心独白型(★★★★★) > 旁白型(★★★★) | 对话型(离域★★) |
| **反转型 + 任意调性** | 对话型(★★★★★) > 手机内容型(★★★★) | 无台词型(离域★★) |
| **幽默轻松 + 休闲/活泼** | 网感型(★★★★★) > 对话型(★★★★) | 旁白型(离域★★) |
```

- [ ] **步骤 4：在维度八节奏模板章节增加映射规则**

在 `#### 2.8.4 钩子-高潮联动矩阵` 后插入：

```markdown

---

#### 节奏模板亲和度映射规则

**上游来源**：第1层·情绪基调 + 用户偏好时长（默认18-20秒）

> **注**：以下为核心映射规则，完整映射表见设计规格文档。

| 情绪基调 + 时长范围 | 推荐节奏模板（优先级排序） | 离域选项（中等概率） |
|---------------------|---------------------------|----------------------|
| **治愈温暖 + 15-18秒** | 单高潮极简型(★★★★★) > 双高潮经典型(★★★) | 反转高潮型(离域★★) |
| **治愈温暖 + 18-20秒** | 双高潮经典型(★★★★★) > 单高潮极简型(★★★★) | 反转高潮型(离域★★) |
| **治愈温暖 + 25-30秒** | 双高潮经典型(★★★★★) > 三高潮完整型(★★★★) | 反转高潮型(离域★★) |
| **热血励志 + 20-25秒** | 递进高潮型(★★★★★) > 三高潮完整型(★★★★) | 情绪波动型(离域★★) |
| **反转型 + 18-22秒** | 反转高潮型(★★★★★) > 双高潮经典型(★★★★) | 单高潮极简型(离域★★) |
| **感伤释怀 + 25-30秒** | 情绪波动型(★★★★★) > 双高潮经典型(★★★★) | 递进高潮型(离域★★) |
```

- [ ] **步骤 5：Commit**

```bash
git add script-creator/SKILL.md
git commit -m "feat(script-creator): 增加表达层维度亲和度映射表（维度2-3-7-8）"
```

---

## 任务 7：增加突破层维度亲和度映射表（维度6-9-10）

**分层归属**：突破层（创意维度）

**文件：**
- 修改：`script-creator/SKILL.md:227-237`（维度六镜头语言）
- 修改：`script-creator/SKILL.md:305-336`（维度九完播钩子）
- 修改：`script-creator/SKILL.md:339-349`（维度十创新元素）

- [ ] **步骤 1：在维度六镜头语言章节增加映射规则**

在镜头语言表格后插入：

```markdown

---

#### 镜头语言亲和度映射规则

**上游来源**：第1层·视觉风格 + 维度1·脚本类型

> **注**：以下为核心映射规则，完整映射表见设计规格文档。

| 视觉风格 + 脚本类型 | 推荐镜头语言（优先级排序） | 离域选项（高概率） |
|---------------------|---------------------------|----------------------|
| **居家温馨 + 氛围感** | 固定镜头(★★★★★) > 慢动作(★★★★) | 手持感(离域★★) |
| **居家温馨 + 日常Vlog** | 手持感(★★★★★) > 固定镜头(★★★★) | 慢动作(离域★★) |
| **日系清新 + 氛围感** | 固定镜头(★★★★★) > 慢动作(★★★★) | 手持感(离域★★) |
| **韩系精致 + 日常Vlog** | 手持感(★★★★★) > 固定镜头(★★★★) | 航拍(离域★★) |
| **都市夜色 + 剧情/短剧** | 固定镜头(★★★★★) > 主观视角(★★★★) | 慢动作(离域★★) |
| **国风雅致 + 氛围感** | 固定镜头(★★★★★) > 慢动作(★★★★★) | 手持感(离域★-不推荐) |
```

- [ ] **步骤 2：在维度九完播钩子章节增加映射规则**

在 `#### 2.9.3 完播钩子选择规则` 后插入：

```markdown

---

#### 完播钩子亲和度映射规则

**上游来源**：维度1·脚本类型 + 第2层·叙事结构

> **注**：以下为核心映射规则，完整映射表见设计规格文档。

| 脚本类型 + 叙事结构 | 推荐完播钩子（优先级排序） | 离域选项（高概率） |
|---------------------|---------------------------|----------------------|
| **剧情/短剧 + 倒叙式** | 悬念钩子(★★★★★) > 问题钩子(★★★★) | 预告钩子(离域★★) |
| **剧情/短剧 + 循环式** | 对比钩子(★★★★★) > 悬念钩子(★★★★) | 问题钩子(离域★★) |
| **氛围感/OOTD + 蒙太奇式** | 预告钩子(★★★★★) | 悬念钩子(离域★★) |
| **日常Vlog + 顺叙式** | 问题钩子(★★★★★) > 预告钩子(★★★★) | 对比钩子(离域★★) |
| **反转类 + 倒叙式** | 对比钩子(★★★★★) > 悬念钩子(★★★★★) | 预告钩子(离域★★) |
```

- [ ] **步骤 3：在维度十创新元素章节增加映射规则**

在创新元素表格后插入：

```markdown

---

#### 创新元素亲和度映射规则

**上游来源**：第1层·情绪基调 + 第2层·叙事结构

**特点**：此维度离域概率最高，鼓励突破常规

> **注**：以下为核心映射规则，完整映射表见设计规格文档。

| 情绪基调 + 叙事结构 | 推荐创新元素（优先级排序） | 离域选项（高概率探索） |
|---------------------|---------------------------|------------------------|
| **治愈温暖 + 顺叙式** | 视觉符号(★★★★) > 时间跳跃(★★★) | 剧情反转(离域★★)、情绪反差(离域★★) |
| **治愈温暖 + 蒙太奇式** | 视觉符号(★★★★★) > 创意转场(★★★★) | 剧情反转(离域★★)、框架叙事(离域★★) |
| **反转型 + 倒叙式** | 剧情反转(★★★★★) > 情绪反差(★★★★) | 视觉符号(离域★★)、时间跳跃(离域★★) |
| **反转型 + 循环式** | 情绪反差(★★★★★) > 框架叙事(★★★★) | 视觉符号(离域★★) |
| **热血励志 + 顺叙式** | 情绪反差(★★★★) > 时间跳跃(★★★) | 剧情反转(离域★★)、创意转场(离域★★) |
| **感伤释怀 + 插叙式** | 时间跳跃(★★★★★) > 情绪反差(★★★★) | 剧情反转(离域★★)、视觉符号(离域★★) |
| **幽默轻松 + 顺叙式** | 情绪反差(★★★★★) > 剧情反转(★★★★) | 视觉符号(离域★★)、时间跳跃(离域★★) |
| **静谧内敛 + 蒙太奇式** | 视觉符号(★★★★★) > 创意转场(★★★★) | 剧情反转(离域★★)、情绪反差(离域★★) |

**创新元素组合规则**：
- 单创新元素：保守策略时选择一个主要创新元素
- 双创新元素组合：中等/大胆策略时，可组合两个创新元素
  - 推荐组合：视觉符号 + 创意转场、情绪反差 + 时间跳跃、框架叙事 + 视觉符号
```

- [ ] **步骤 4：Commit**

```bash
git add script-creator/SKILL.md
git commit -m "feat(script-creator): 增加突破层维度亲和度映射表（维度6-9-10）"
```

---

## 任务 8：增加上游输入缺失处理和Fallback策略

**文件：**
- 修改：`script-creator/SKILL.md`（在阶段2末尾，阶段3之前插入）

- [ ] **步骤 1：在阶段2末尾、阶段3之前插入新章节**

在 `### 2.10 维度十：创新元素` 章节结束后，`## 阶段3：脚本生成` 之前插入：

```markdown
---

### 2.11 上游输入缺失处理规则

当上游分析不完整时，应用以下 fallback 规则：

| 缺失情况 | Fallback 规则 |
|----------|---------------|
| **缺少热点分析** | 仅依赖角色分析，脚本类型默认为"氛围感/OOTD"或"日常Vlog"，情绪基调根据服饰调性推断 |
| **缺少角色分析** | 仅依赖热点分析，服饰风格假设为"百搭基础风"，场景设定根据热点推演直接确定 |
| **两者都缺少** | 使用默认值：脚本类型=氛围感/OOTD，情绪基调=治愈温暖型，视觉风格=日系清新，场景=居家窗边 |

---

### 2.12 映射表无匹配时的Fallback策略

当维度选择时在映射表中找不到精确匹配的组合，应用以下策略：

#### 通用Fallback原则

1. **情绪基调优先**：若组合中有情绪基调因素，优先按情绪基调查找单独的映射表
2. **脚本类型次之**：若组合中有脚本类型因素，按脚本类型查找通用推荐
3. **默认值兜底**：若以上都无效，使用各维度的默认推荐

#### 各维度Fallback默认推荐

| 维度 | 默认推荐 | 离域选项 |
|------|----------|----------|
| 维度1：脚本类型 | 氛围感/OOTD | 日常Vlog |
| 维度2：开场钩子类型 | 视觉冲击型 | 悬念型 |
| 维度3：叙事结构 | 顺叙式 | 蒙太奇式 |
| 维度4：情绪基调 | 治愈温暖型 | 静谧内敛型 |
| 维度5：视觉风格 | 日系清新 | 居家温馨 |
| 维度6：镜头语言 | 固定镜头 | 慢动作 |
| 维度7：台词风格 | 内心独白型 | 无台词型 |
| 维度8：节奏模板 | 双高潮经典型 | 单高潮极简型 |
| 维度9：完播钩子设计 | 预告钩子 | 问题钩子 |
| 维度10：创新元素 | 视觉符号 | 创意转场 |
```

- [ ] **步骤 2：Commit**

```bash
git add script-creator/SKILL.md
git commit -m "feat(script-creator): 增加上游输入缺失处理和Fallback策略"
```

---

## 任务 9：更新输出格式（增加来源和亲和度列）

**文件：**
- 修改：`script-creator/SKILL.md:371-385`（多样化维度表格位置）

- [ ] **步骤 1：更新多样化维度表格格式**

将原有的：
```markdown
## 多样化维度

| 维度 | 选择 |
|------|------|
| 脚本类型 | [类型] |
...
```

替换为：

```markdown
## 多样化维度

| 维度 | 选择 | 来源 | 亲和度 |
|------|------|------|--------|
| 脚本类型 | [类型] | 热点推荐/调整 | ★★★★★ |
| 开场钩子 | [类型] | 表达层推荐/离域 | ★★★★★/★★★ |
| 叙事结构 | [结构] | 表达层推荐/离域 | ★★★★★/★★★ |
| 情绪基调 | [基调] | 核心层推荐 | ★★★★★ |
| 视觉风格 | [风格] | 核心层推荐 | ★★★★★ |
| 镜头语言 | [语言] | 创意层推荐/离域 | ★★★★★/★★ |
| 台词风格 | [风格] | 表达层推荐/离域 | ★★★★★/★★★ |
| 节奏模板 | [模板 + 高潮分布] | 表达层推荐 | ★★★★★ |
| 完播钩子 | [类型 + 位置] | 创意层推荐/离域 | ★★★★/★★ |
| 创新元素 | [元素] | 创意层推荐/离域组合 | ★★★★/★★ |

**创意适配度等级**：[保守/中等/大胆]（[判定依据]）
**离域情况**：表达层[N]次离域，突破层[N]次离域组合
```

- [ ] **步骤 2：更新示例中的多样化维度表格**

在示例部分（约第607-621行），更新表格格式：

```markdown
## 多样化维度

| 维度 | 选择 | 来源 | 亲和度 |
|------|------|------|--------|
| 脚本类型 | 氛围感/OOTD | 热点推荐 | ★★★★★ |
| 开场钩子 | 视觉冲击型 | 表达层推荐 | ★★★★★ |
| 叙事结构 | 顺叙式 | 表达层推荐 | ★★★★★ |
| 情绪基调 | 治愈温暖型 | 核心层推荐 | ★★★★★ |
| 视觉风格 | 居家温馨 | 核心层推荐 | ★★★★★ |
| 镜头语言 | 固定镜头 | 创意层推荐 | ★★★★★ |
| 台词风格 | 内心独白型 | 表达层推荐 | ★★★★★ |
| 节奏模板 | 单高潮极简型（Lv4高潮在第8-14秒） | 表达层推荐 | ★★★★★ |
| 完播钩子 | 预告钩子（第6-8秒位置） | 创意层推荐 | ★★★★ |
| 创新元素 | 视觉符号（窗边阳光） | 创意层推荐 | ★★★★ |

**创意适配度等级**：保守（情绪复杂度=中，热点新鲜度=低）
**离域情况**：无离域
```

- [ ] **步骤 3：Commit**

```bash
git add script-creator/SKILL.md
git commit -m "feat(script-creator): 更新输出格式，增加来源和亲和度列"
```

---

## 任务 10：最终验证和文档整理

**文件：**
- 修改：`script-creator/SKILL.md`

- [ ] **步骤 1：更新快速导航的行号**

检查并更新快速导航中的行号，确保与新内容匹配。

- [ ] **步骤 2：验证所有新增内容**

**检查清单**：
- [ ] 综合评分引擎章节已添加（含五因素表、判定表、判定示例）
- [ ] 三层离域倾向等级已添加
- [ ] 连续生成离域触发规则已添加
- [ ] 场景智能推导规则已添加（含映射表、冲突处理、推导示例）
- [ ] 10个维度都有亲和度映射规则
- [ ] 亲和度星级含义已在核心层章节说明
- [ ] 上游输入缺失处理规则已添加
- [ ] Fallback策略已添加
- [ ] 输出格式已更新（含示例）

- [ ] **步骤 3：验证格式一致性**

检查：
- [ ] 所有表格格式正确
- [ ] 代码块缩进正确
- [ ] 章节层级正确（### 用于子章节，#### 用于更小节）
- [ ] 所有映射表都有"注：完整映射表见设计规格文档"说明

- [ ] **步骤 4：最终 Commit**

```bash
git add script-creator/SKILL.md
git commit -m "docs(script-creator): 更新快速导航和格式整理"
```

---

## 完成标准

- [ ] 所有任务完成
- [ ] 所有 commit 已推送
- [ ] 文档格式正确，无语法错误
- [ ] 新增内容与原有内容风格一致
- [ ] 每个维度都有完整的亲和度映射规则
- [ ] 设计规格中的核心内容都已整合到 SKILL.md